//Crie um script que declare uma função que receba um array de números como parâmetro e retorne o menor número do array. 
//Utilize a função Math.min() para encontrar o menor número.

const numeros = [10, 30, 35, 18];

var menorNumero = Math.min(numeros[0], numeros[1], numeros[2], numeros[3]);
console.log(`O menor número do array é: ${menorNumero}`);